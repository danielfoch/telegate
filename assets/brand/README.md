# Telegate brand assets

`telegate-original.png` is the logo supplied by Daniel. `telegate-icon.png` is its approved-direction green and near-black colour adaptation for the apps and website. Keep the existing UI palettes.

Run `swift scripts/make-icons.swift "$PWD"` from the repository root to regenerate platform icon sizes from the canonical colour asset.
